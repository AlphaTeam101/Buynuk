import 'package:flutter/material.dart';

class ThemeAnimationWrapper extends StatelessWidget {
  final Widget child;

  const ThemeAnimationWrapper({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
